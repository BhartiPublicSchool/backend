{
  "routes": [
    {
      "method": "GET",
      "path": "/faculty",
      "handler": "faculty.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/faculty",
      "handler": "faculty.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/faculty",
      "handler": "faculty.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
